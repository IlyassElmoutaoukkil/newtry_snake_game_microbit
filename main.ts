let score = 1
let move = 'toRight'
let snakePosition = [0,0]

basic.forever(function () {
    let leds = []
    let s = 0

	if(move=='toRight'){
        while (s < score) {
            s++
            let ledNewPosition = [[snakePosition[0]++, [snakePosition[1]]
            leds.push(ledNewPosition)
        }
    }
})
